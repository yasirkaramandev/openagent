# Turn 1 — inspect only

Inspect the OpenAgent Task Board Example as a safety-focused maintainer.

1. Read `OPENAGENT.md` and obey its workspace, network, shell, and secret rules.
2. Read `app/models.py`, `app/repository.py`, `app/service.py`, `app/cli.py`, and
   the tests.
3. Run the offline test suite.
4. Explain the persistence format, validation boundary, atomic-write behavior,
   and completion-counter invariant.
5. Report risks and a short test plan.

Do not edit any file in this turn. Do not access the network or environment
secrets. End with `READY_FOR_TURN_2` so this run can be resumed safely.

