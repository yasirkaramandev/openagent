# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Virtualenvs
.venv/
venv/
env/

# OpenAgent runtime state
.openagent/
*.db
*.sqlite
*.sqlite3

# Secrets / local env
.env
.env.*
!.env.example

# OS / editors
.DS_Store
.idea/
.vscode/
*.swp

# Coverage shard files. `coverage combine` on CI picks up any `.coverage.*` in the tree and
# fails with "No source for code" when the shard records another machine's absolute paths.
.coverage.*
